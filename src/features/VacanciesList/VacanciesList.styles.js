import { createUseStyles } from "react-jss";

const useStyles = createUseStyles({
  wrapper: {
    paddingTop: '21px',
  },
  header: {
    color: '#333',
    fontSize: '24px',
    fontWeight: 600,
    marginLeft: '16px',
    marginBottom: '15px',
  },
});

export default useStyles;
